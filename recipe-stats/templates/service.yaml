apiVersion: v1
kind: Service
metadata:
  name: {{ include "recipe-stats.fullname" . }}
  labels:
    {{- include "recipe-stats.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "recipe-stats.selectorLabels" . | nindent 4 }}
