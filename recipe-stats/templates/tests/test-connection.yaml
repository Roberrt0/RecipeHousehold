apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "recipe-stats.fullname" . }}-test-connection"
  labels:
    {{- include "recipe-stats.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "recipe-stats.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
